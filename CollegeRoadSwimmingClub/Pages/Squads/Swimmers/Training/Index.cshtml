@page
@model CollegeRoadSwimmingClub.Pages.Squads.Swimmers.Training.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrainingResult[0].Swimmer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainingResult[0].Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainingResult[0].Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainingResult[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TrainingResult) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Swimmer.Address1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
